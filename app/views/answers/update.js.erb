<% if @answer.errors.blank? %>
  $('.row-answer-<%= @answer.id %>-errors').replaceWith("")
  $('.row-answer-<%= @answer.id %>').replaceWith('<%= j render @answer %>');
<% else %>
    $('.row-answer-<%= @answer.id %>-errors').html("<%= render 'shared/errors', resource: @answer %>");
<% end %>

