<% if @answer.errors.blank? %>
  $('.answers').html('<%= j render 'answers/table_header' %><%= j render @question.answers.with_attached_files %>');
<% else %>
    $('.answer-<%= @answer.id %>-errors').html("<%= render 'shared/errors', resource: @answer %>");
<% end %>

