<% if @question.errors.blank? %>
    $('.row-question-<%= @question.id %>').replaceWith('<%= j render @question %>');
<% else %>
    $('.question-<%= @question.id %>-errors').html("<%= render 'shared/errors', resource: @question %>");
<% end %>
